# Q3CC-api-doc

VitePress documentation site for `api.q3cc.top`.

## Commands

```bash
npm install
npm run docs:dev
npm run build
npm run preview
```

## Deploy

Deploy to Cloudflare Pages and bind the custom domain:

- `api.q3cc.top`
